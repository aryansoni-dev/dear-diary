# Onboarding screen-3

This is a code bundle for Onboarding screen-3. It's based on default Vite project. The original design is available at https://app.flowstep.ai/file?activeFileId=2944626c-0c0b-4416-8a9e-4637c2e3df0b.

## Running the code

Run `npm i` and `npm run setup` to install the dependencies.

Run `npm run dev` to start the development server.
